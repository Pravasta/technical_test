import 'package:flutter/material.dart';
import 'section/register_view.dart';

class Registerpage extends StatelessWidget {
  const Registerpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: RegisterView(),
    );
  }
}